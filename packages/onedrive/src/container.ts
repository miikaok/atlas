import { type Container } from 'inversify';

/** Registers OneDrive-specific DI bindings. Stub -- no bindings yet. */
export function bind_onedrive(_container: Container): void {
  // TODO: register OneDrive adapters and services when implemented
}
