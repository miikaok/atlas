import { type Container } from 'inversify';

/** Registers SharePoint-specific DI bindings. Stub -- no bindings yet. */
export function bind_sharepoint(_container: Container): void {
  // TODO: register SharePoint adapters and services when implemented
}
